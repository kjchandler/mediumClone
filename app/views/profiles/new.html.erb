<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <div class="panel">
        <h3>Welcome to braInstagram!</h3>
        <p>
          We're going to have to ask you a few questions.
        </p>
        <p>
          Namely: what should we call you?
        </p>
        <%= form_for(@profile) do |f| %>
        <% if @profile.errors.count > 0 %>
          <div class="alert alert-danger">
            <% @profile.errors.full_messages.each do |e| %>
            <ul>
              <li><%= e %></li>
            </ul>
            <% end %>
          </div>
        <% end %>
        <div class="field form-group">
          <%= f.text_field :username, :class => 'form-control', :placeholder => "Username" %>
        </div>
        <p>
          Lastly (and we're sorry if this is a bit creepy), but what do you look like?
        </p>
        <div class="field form-group">
          <%= f.file_field :avatar %>
        </div>
        <%= f.submit "Let's go!", :class => 'btn btn-primary' %>
        <% end %>


    </div>
  </div>
</div>